node_modules/
dist/
build/
out/
coverage/
.turbo/
.next/
.expo/
.docusaurus/
pnpm-lock.yaml
*.tsbuildinfo

# Fichiers de configuration en texte brut (format INI-like ou liste de
# versions) : Prettier n'a pas de parser pour eux et échoue en tentant de
# les lire comme du JS. Ce ne sont pas des oublis, ce sont des formats que
# Prettier ne connaît structurellement pas.
.npmrc
.nvmrc
.env
.env.*

# Certaines leçons contiennent volontairement du code MAL formaté :
# c'est l'exercice. Prettier ne doit pas « corriger » l'énoncé.
**/fixtures/badly-formatted/**
